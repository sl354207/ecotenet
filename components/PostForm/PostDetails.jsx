const PostDetails = ({ handleNext, handleChange, values: { firstName, lastName, email, gender } }) => {
    return (
        <div>
            <h1>hello</h1>
        </div>
    )
}

export default PostDetails
